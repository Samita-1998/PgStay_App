## Number Fields Rule
- All number fields across the entire project must NEVER contain or allow negative values. Use `FilteringTextInputFormatter.digitsOnly` or equivalent where applicable to prevent users from typing the minus sign.
